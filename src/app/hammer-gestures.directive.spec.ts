import { HammerGesturesDirective } from './hammer-gestures.directive';

describe('HammerGesturesDirective', () => {
  it('should create an instance', () => {
    const directive = new HammerGesturesDirective();
    expect(directive).toBeTruthy();
  });
});
